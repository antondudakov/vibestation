# 13: `--help` and README

**What to build:** A user who has just installed the tool can orient themselves
without reading the source. `--help` explains what vibestation does and what its
flags are; the README describes the real, finished behaviour — the picker, the
naming convention, the worktree workflow, the config file and its settings, and
how to install.

**Blocked by:** 10, 11, 12.

**Status:** ready-for-agent

- [ ] `--help` explains what the tool does, not just its flags
- [ ] `--version` reports the installed version, so bug reports are useful
- [ ] The README covers installation for macOS and Linux, the picker, branch and session naming, the worktree workflow, and every config setting
- [ ] The README's described behaviour matches what the tool actually does
